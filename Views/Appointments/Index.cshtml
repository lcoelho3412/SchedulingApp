@model List<SchedulingApp.Models.Appointment>

<h2>Appointments</h2>

<a asp-action="Create">Create New Appointment</a>
<br /><br />


<ul>
  @foreach (var appointment in Model)
  {
    <li>
      <strong>@appointment.Title</strong><br />
      @appointment.ClientName — @appointment.Date.ToShortDateString()
      <br />
      <a asp-action="Edit" asp-route-id="@appointment.Id">Edit</a>
      <a asp-action="Delete" asp-route-id="@appointment.Id">Delete</a>

    </li>
  }

</ul>